import { FC } from 'react';

import { DisconnectButton } from './components/DisconnectButton';

export const App: FC = () => {
    return (
        <div>
            Navbar HEre
            <br />
            Routing here
            <DisconnectButton />
        </div>
    );
};
