import { PageWrapper } from '../../common/components/page-wrapper/PageWrapper';

const Settings = () => {
    return (
        <PageWrapper>
            <h1>Settings</h1>
        </PageWrapper>
    );
};

export default Settings;
